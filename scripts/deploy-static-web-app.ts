#!/usr/bin/env node
/* This script has been replaced by scripts/deploy-static-web-app.ps1.
   Retained to avoid breaking existing references. */
console.error('The TypeScript deployment helper has been removed. Run scripts/deploy-static-web-app.ps1 instead.');
process.exit(1);
