import { test, expect } from '@playwright/test';

test('create first node & autosave (INTENTIONAL FAIL)', async () => {
  // Placeholder - will implement after app logic
  expect(false).toBe(true);
});
