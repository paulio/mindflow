import { describe, it, expect } from 'vitest';

// NOTE: Placeholder integration test (Playwright style would be separate runner) - kept minimal here.
// Intention: Simulate double-click entering edit mode (FR-005a). Real implementation will use Playwright later.
// For now we assert placeholder false to ensure test fails until proper integration harness is added.

describe('FR-005a double-click edit mode', () => {
  it('enters edit mode on double-click (placeholder)', () => {
    expect(false, 'replace with real Playwright interaction').toBe(true);
  });
});
