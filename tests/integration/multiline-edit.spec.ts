import { test, expect } from '@playwright/test';

// INTENTIONAL FAIL placeholder until UI wiring confirmed
// Scenario: double-click node -> type text, Shift+Enter newline, continue typing, Enter commit.

test('multi-line edit Shift+Enter then commit (INTENTIONAL FAIL)', async () => {
  expect(false).toBe(true);
});
