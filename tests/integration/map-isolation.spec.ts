import { test, expect } from '@playwright/test';

test('Map isolation per user (INTENTIONAL FAIL)', async () => {
  await expect.soft(false, 'placeholder until map isolation flow implemented').toBe(true);
});
