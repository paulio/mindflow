import { describe, it, expect } from 'vitest';

describe('Edit mode idempotency + no-op Enter', () => {
  it('double-click while editing has no effect; Enter with no change no-op (placeholder)', () => {
    expect(false, 'placeholder failing test until implemented').toBe(true);
  });
});
