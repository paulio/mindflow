import { test, expect } from '@playwright/test';

test('Entra login flow (INTENTIONAL FAIL)', async () => {
  await expect.soft(false, 'placeholder until Entra login flow implemented').toBe(true);
});
